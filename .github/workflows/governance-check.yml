name: Governance Check
on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:
jobs:
  audit:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - name: Check branch protections
        run: gh api repos/${{ github.repository }}/branches/main/protection
